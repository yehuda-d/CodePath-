version: '3.8'

services:
  db:
    image: postgres:16
    container_name: codepath-db
    environment:
      POSTGRES_PASSWORD: devpassword
      POSTGRES_DB: codepath
    ports:
      - "5432:5432"
    volumes:
      - codepath-data:/var/lib/postgresql/data

volumes:
  codepath-data: